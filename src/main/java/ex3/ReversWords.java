package ex3;

public class ReversWords implements ReverseLetters {

    @Override
    public String apply(String from) {
        return null;
    }

    public static String reverseWord(String word) {
        return new StringBuilder(word).reverse().toString();
    }
}
